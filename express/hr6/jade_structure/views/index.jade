extends layout

block content
	h1= title
		p Welcome to #{title}
	section#wrapper
		h2 Basic Structure
			section
				p 
				span this is a span whthin a p within a div！

	-var name = "Carol"
	h1 Hello #{name}!

	-beatles=['John','Paul', 'Ringo', 'George'] 
		ul 
			each beatle in beatles
				li #{beatle}

	-raining = false
	-if(raining)
		p It is raining. Take an umbrella.
	-else
		p No rain. Take the bike!


	script alert('Inline JS in jade')

	mixin users(users)
		ul
			each user in users
				li=user

	-users=['John','Paul', 'Ringo', 'George'] 
	
	mixin users(beatles)

	h2 Form example
	form(method='post',action='/')
		fieldset
			legend Add a user
			p
				label Frist name
				input(name='user[first_name]')
			p
				label Last name
				input(name='user[surname]')
			p
				input(type='submit',value='Save')

	h1 Passing data through to the view layer

	h3 First Name
	p=user.first_name

	h3 Surname
	p=user.sur_name

	h3 Address
	p=user.address

	h3 Facebook Friends
	p=user.facebook_friends

block footer
	include footer